<FormField
  @isSmall={{@isSmall}}
  @isLarge={{@isLarge}}
  class="form-checkbox-group
    {{if @isInline "form-checkbox-group--inline"}} {{@containerClass}}
    {{if @isSmall "form-checkbox-group--sm" (if @isLarge "form-checkbox-group--lg")}}"
    role="group"
  ...attributes
  as |f|
>
  {{#if @label}}
    <f.Label>{{@label}}</f.Label>
  {{/if}}

  {{#if @hint}}
    <f.Hint>{{@hint}}</f.Hint>
  {{/if}}

  {{yield (component "form-checkbox"
            _parentOnChange=this.handleChange
            isSmall=@isSmall
            isLarge=@isLarge
          )
          (hash onChange=this.handleChange)
  }}

  {{#if this.showErrorFeedback}}
    <f.Feedback @errors={{@errors}} />
  {{/if}}
</FormField>
