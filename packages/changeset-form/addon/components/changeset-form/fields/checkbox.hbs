<FormCheckbox
  @onChange={{this.handleChange}}
  @checked={{this.value}}
  @hint={{@hint}}
  @name={{@name}}
  @containerClass={{@containerClass}}
  @size={{@size}}
  ...attributes
>
  {{#if has-block}}
    {{yield}}
  {{else}}
    {{@label}}
  {{/if}}
</FormCheckbox>