<FormRadio
  {{on "blur" this.validate}}

  @onChange={{this.handleChange}}
  @checked={{this.value}}

  @hint={{@hint}}
  @value={{@value}}
  @name={{@name}}
  @containerClass={{@containerClass}}
  @size={{@size}}

  ...attributes
>
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{@label}}
  {{/if}}
</FormRadio>
