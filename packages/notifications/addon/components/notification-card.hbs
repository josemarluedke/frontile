{{!-
appearances:
  notification-card-info
  notification-card-success
  notification-card-warning
  notification-card-error
-}}

<div
  {{on "mouseenter" this.pause}}
  {{on "mouseleave" this.resume}}
  class="notification-card notification-card-{{@notification.appearance}} {{if @notification.isRemoving "opacity-0"}}"
  ...attributes
>
  <div class="message">
    {{@notification.message}}
  </div>

  {{#if @notification.customActions}}
    <div class="custom-actions">
      {{#each @notification.customActions as |customAction|}}
        <button
          type="button"
          class="custom-action-btn"
          {{on "click" (fn this.handleClickCustomAction customAction)}}
        >
          {{customAction.label}}
        </button>
      {{/each}}
    </div>
  {{/if}}

  {{#if @notification.allowClosing}}
    <button
      type="button"
      class="close-btn"
      {{on "click" this.remove}}
    >
      <div class="close-btn-icon"></div>
      <VisuallyHidden>Close</VisuallyHidden>
    </button>
  {{/if}}
</div>
