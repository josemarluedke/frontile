---
title: llms.txt & Markdown
order: 2
category: get-started
subcategory: ai
---

# llms.txt & Markdown

Every page of these docs is published twice: once as the HTML you are reading, and once as
plain Markdown for anything that would rather not parse a single-page app. Alongside them
sits an index and a set of bulk files, following the [llms.txt standard](https://llmstxt.org).

All of it is generated by the documentation build. Nothing is hand-maintained, so nothing
drifts from the site.

## Per-page Markdown mirrors

Any page URL plus `.md` returns that page as Markdown:

```
/docs/components/buttons/button      →  the HTML page
/docs/components/buttons/button.md   →  the same content, as Markdown
```

The mirror is not a stripped-down summary. Component pages carry the full generated API
table: every argument, its type, its default, and a Deprecated marker with the
migration instruction where one applies. The `<Signature />` tag is expanded into a real
Markdown table before export, so nothing arrives as an unresolved component tag.

Fetch the `.md`. Fetching the HTML page and stripping tags is strictly worse and gets the
API table wrong.

## `llms.txt`

The index: every page, grouped by section, each as a link to its `.md` mirror.

```
## Buttons

- [Button](/docs/components/buttons/button.md): The base pressable action, with colors, variants, and sizes.
- [Chip](/docs/components/buttons/chip.md): A compact label for statuses, filters, and removable tags.
```

Component entries carry a one-line description, so the right component can usually be chosen
without fetching anything else. Guide pages are listed without one.

The file opens with a preamble covering the conventions that are easiest to get wrong:
`@color` versus the deprecated `@intent`, the absence of a numbered color scale, the fact
that some Utilities pages document template modifiers rather than components. Sections that
are deprecated are marked as such, so `Forms (Legacy)` cannot be mistaken for current
`Forms`.

## Bulk files

`llms-full.txt` concatenates every page. It is comprehensive and about 1.2 MB, which is more
than most agents will load in one request. Three topic-scoped files exist for that reason:

| File                     | Contents                              | Approx. size |
| ------------------------ | ------------------------------------- | ------------ |
| [`/llms.txt`](/llms.txt) | Index only                            | 12 KB        |
| `/llms-components.txt`   | Every component page                  | 900 KB       |
| `/llms-theming.txt`      | Theming, design tokens, configuration | 125 KB       |
| `/llms-migrations.txt`   | Migration guides                      | 110 KB       |
| `/llms-full.txt`         | Everything                            | 1.2 MB       |

Prefer the narrowest file that covers the task. An agent working on a component never needs
the migration prose.

## The component inventory

`/component-inventory.json` lists every current component with its path and a one-line
description, the same data behind the [components overview](/docs/components/overview).
Useful when something needs the catalogue as structured data rather than as prose.

Legacy packages are deliberately excluded from it.

## Version-matched origins

These files are built per version, so each documentation origin serves its own. Only
versions built after the export existed have them:

| Origin                                                    | Agent surface    |
| --------------------------------------------------------- | ---------------- |
| [`next.frontile.dev`](https://next.frontile.dev/llms.txt) | Yes              |
| `frontile.dev`                                            | From 0.18 onward |
| `v0.16.frontile.dev`                                      | No               |

An origin without them returns the site's HTML shell with **HTTP 200**, not a 404. Check the
body, not the status code: Markdown that begins with `<!DOCTYPE html>` is a failed fetch.

For anything version-sensitive, such as an argument's exact type or whether it is deprecated, read
`node_modules/frontile/declarations/**/*.d.ts` in the consuming app instead. That is exact
for the installed version by construction.
